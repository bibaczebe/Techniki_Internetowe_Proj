@* Strona z formularzem dodawania przepisu.
   Składniki wpisuje się w polu tekstowym z podpowiedziami (datalist z bazy JSON).
   Formularz wysyła dane na serwer (POST Home/Add): przepis zapisuje się i od razu
   pojawia na stronie głównej. JavaScript obsługuje tylko dynamiczne dodawanie składników. *@
@{
    ViewData["Title"] = "Dodaj przepis";
    var categories = ViewBag.Categories as List<Category> ?? new List<Category>();
    var units = ViewBag.Units as List<string> ?? new List<string>();
}

<section class="form-page">
    <h1>Dodaj przepis</h1>
    <p class="form-note">
        Po zapisaniu przepis pojawi się na stronie głównej. Zdjęcie jest opcjonalne —
        gdy go nie dodasz, użyjemy domyślnej grafiki.
    </p>

    @* Komunikaty błędów z serwera (np. zbyt krótka nazwa) *@
    <div asp-validation-summary="All" class="auth-validation"></div>

    <form id="addRecipeForm" class="recipe-form" asp-controller="Home" asp-action="Add" method="post" enctype="multipart/form-data">
        <div class="form-row">
            <label for="recipeTitle">Nazwa dania</label>
            <input type="text" id="recipeTitle" name="Title" placeholder="np. Omlet z warzywami" required />
        </div>

        <div class="form-grid-2">
            <div class="form-row">
                <label for="recipeCategory">Kategoria</label>
                <select id="recipeCategory" name="Category" required>
                    @foreach (var c in categories)
                    {
                        <option value="@c.Name">@c.Name</option>
                    }
                </select>
            </div>
            <div class="form-row">
                <label for="recipeTime">Czas przygotowania (min)</label>
                <input type="number" id="recipeTime" name="PrepTime" min="1" value="30" required />
            </div>
        </div>

        @* Pole na zdjęcie dodane przez kolegę (backend) — zostawiamy *@
        <div class="form-row">
            <label for="imageFile">Zdjęcie przepisu</label>
            <input type="file" id="imageFile" name="imageFile" accept="image/*" class="form-control" />
        </div>

        <fieldset class="ingredients-fieldset">
            <legend>Składniki</legend>
            @* Tutaj JavaScript dodaje kolejne wiersze składników *@
            <div id="ingredientList"></div>
            <button type="button" id="addIngredientBtn" class="btn-add-ingredient">+ Dodaj składnik</button>
        </fieldset>

        <div class="form-row">
            <label for="recipeSteps">Kroki przygotowania (jeden krok w linii)</label>
            <textarea id="recipeSteps" name="Steps" rows="4" placeholder="Pokrój warzywa...&#10;Usmaż na patelni..."></textarea>
        </div>

        <button type="submit" class="hero-btn hero-btn-primary">Dodaj przepis</button>
    </form>

    @* Lista podpowiedzi składników (wypełnia ją JavaScript z pliku JSON) *@
    <datalist id="ingredientsDatalist"></datalist>

    @* Szablon jednego wiersza składnika — JavaScript go klonuje przy dodawaniu.
       Atrybuty name="..." sprawiają, że po sklonowaniu wiersze wysyłają się jako tablice. *@
    <template id="ingredientRowTemplate">
        <div class="ingredient-row">
            <input type="text" class="ing-name" name="IngredientNames" list="ingredientsDatalist" placeholder="wpisz składnik..." aria-label="Składnik" />
            <input type="number" class="ing-amount" name="IngredientAmounts" min="0" step="any" value="1" aria-label="Ilość" />
            <select class="ing-unit" name="IngredientUnits" aria-label="Jednostka">
                @foreach (var u in units)
                {
                    <option value="@u">@u</option>
                }
            </select>
            <button type="button" class="ing-remove" aria-label="Usuń składnik">✕</button>
        </div>
    </template>
</section>

@section Scripts {
    <script src="~/js/ingredients-datalist.js" asp-append-version="true"></script>
    <script src="~/js/add-recipe.js" asp-append-version="true"></script>
}
